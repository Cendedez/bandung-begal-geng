# The browser always calls the same-origin /api proxy.
# Do not set NEXT_PUBLIC_API_BASE_URL in Vercel for this app.

# Set this in Vercel Project Settings, not in a committed file.
# INTERNAL_API_URL=https://api-staging.example.com
